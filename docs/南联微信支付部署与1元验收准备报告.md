# 南联微信支付部署与1元验收准备报告

检查日期：2026-08-28

## 结论

当前本地代码已经完成生产部署准备，但正式 CloudBase 环境尚未达到真实付款条件，必须保持支付关闭。

- `clubApi` 云端最后更新时间：2026-08-27 23:25:47，尚未部署本轮代码。
- 云函数支付环境变量：未配置。
- `PAYMENT_PRODUCTION_ENABLED`：环境变量未配置，代码默认等效为 `false`。
- HTTP回调：未部署，公网 POST 探测返回 HTTP 404。
- 数据库支付集合：当前仍为“仅创建者可读写”，不满足客户端禁止写入要求，属于 P0。
- 真实付款：不允许。

## 本地完成项

- 固定公网回调路由 `POST /wechat-pay/notify`，不依赖小程序登录态。
- `paymentEnvironmentCheck()` 只返回 `READY/NOT_READY` 和缺失项标识。
- `Wechatpay-Serial` 按实际值选择验签公钥，兼容微信支付公钥 ID 与平台证书序列号。
- 支持可选 `WECHAT_PAY_VERIFICATION_KEYS_JSON` 多公钥映射，便于灰度和轮换。
- “支付测试套餐”：1节、100分、1个月、默认 `INACTIVE`，测试订单禁止优惠券。
- ADMIN“微信支付状态”页面、测试订单事件诊断和A-G只读验收检查器。
- 六个支付关键集合的客户端零权限规则文件。

## 推荐回调地址

```text
https://cloud1-d2g4gi77g48dcee01.ap-shanghai.app.tcloudbase.com/wechat-pay/notify
```

需要在 CloudBase HTTP访问服务中把该 POST 路由映射到 `clubApi`，并关闭身份认证。非微信签名探测应返回401/400，不能返回404。

## 自动化验证

- 当前44个测试脚本全部通过。
- WeChat payment closed-loop regression：20 scenarios passed。
- WeChat payment production readiness regression：15 checks passed。
- Registered pages：70。
- Missing page files：0。
- 小程序包体：671.2KB / 2048KB。

## 人工阻塞项

1. 在正式环境配置全部微信支付变量，其中生产开关明确填写 `false`。
2. 部署本轮 `clubApi` 代码。
3. 创建公网 HTTP POST 路由并复测。
4. 将支付关键集合改为“小程序所有用户不可读写”。
5. 确认诊断页为 `READY`，但生产开关仍为关闭。
6. 经人工确认后再开启1元测试套餐与生产支付开关，执行小额真实付款验收。
