const api = require("../../utils/api"); Page({ data: { rows: [], loading: true, error: "" }, onShow() { this.load(); }, async load() { this.setData({ loading: true, error: "" }); try { const rows = await api.call("listWeeklyTrainingPlans"); this.setData({ rows: rows.map((item) => ({ ...item, focusText: (item.trainingFocus || []).join(" / ") })), loading: false }); } catch (error) { this.setData({ loading: false, error: error.message || "周训练计划加载失败" }); } }, add() { wx.navigateTo({ url: "/pages/training-plan-detail/index" }); }, edit(event) { wx.navigateTo({ url: `/pages/training-plan-detail/index?id=${event.currentTarget.dataset.id}` }); } });
