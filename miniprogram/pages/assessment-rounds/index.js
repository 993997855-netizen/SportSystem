const api = require("../../utils/api");
Page({ data: { rows: [], role: "coach", loading: true, error: "" }, onShow() { this.load(); }, async load() { this.setData({ loading: true, error: "" }); try { const [context, rows] = await Promise.all([api.call("getContext"), api.call("listAssessmentRounds")]); this.setData({ role: context.user.role, rows, loading: false }); } catch (error) { this.setData({ loading: false, error: error.message || "阶段测评加载失败" }); } }, open(event) { wx.navigateTo({ url: `/pages/assessment-round-detail/index?id=${event.currentTarget.dataset.id}` }); }, add() { wx.navigateTo({ url: "/pages/assessment-round-form/index" }); } });
