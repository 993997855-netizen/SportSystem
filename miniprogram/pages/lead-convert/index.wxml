<view class="page page-enter" wx:if="{{lead}}"><view class="card"><view class="lead-name">{{lead.childName}}</view><view class="muted lead-meta">{{lead.gender}} · {{lead.birthday}} · {{lead.school}} · {{lead.grade}}</view><view class="divider"></view><view class="between"><text class="muted">家长</text><text>{{lead.parentName}}</text></view><view class="between info"><text class="muted">手机号</text><text>{{lead.mobile}}</text></view><view class="between info"><text class="muted">招生来源</text><text>{{lead.source}}</text></view><view class="between info"><text class="muted">备注</text><text>{{lead.remark || '无'}}</text></view></view><view class="card"><view class="field"><text class="field-label">普通正式班 *</text><checkbox-group bindchange="classes"><label class="check" wx:for="{{classes}}" wx:key="id"><checkbox value="{{item.id}}" checked="{{item.checked}}" color="#173B77"/>{{item.name}}（{{item.studentCount}}/{{item.standardCapacity}}）</label></checkbox-group></view><view class="field"><text class="field-label">购买套餐 / 初始课时</text><picker range="{{packages}}" range-key="name" value="{{packageIndex}}" bindchange="pack"><view class="picker">{{packages[packageIndex].name}} · {{packages[packageIndex].lessons}}节 · ¥{{packages[packageIndex].amount}}</view></picker></view><view class="field"><text class="field-label">报名日期</text><picker mode="date" value="{{registrationDate}}" bindchange="registrationDate"><view class="picker">{{registrationDate}}</view></picker></view><view class="field"><text class="field-label">招生负责人</text><picker range="{{coaches}}" range-key="name" value="{{coachIndex}}" bindchange="coach"><view class="picker">{{coaches[coachIndex].name || lead.ownerCoachName}}</view></picker></view><view class="notice">CRM 转正式学员只能进入普通班；精英队需后续走推荐或管理员选拔流程。期初课时流水与 CRM 历史均会保留。</view></view><button class="btn-primary" loading="{{saving}}" bindtap="save">确认转为正式学员</button></view>
