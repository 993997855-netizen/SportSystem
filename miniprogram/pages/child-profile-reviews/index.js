const api=require("../../utils/api"); Page({data:{rows:[],loading:true},onShow(){this.load();},onPullDownRefresh(){this.load();},async load(){this.setData({loading:true});try{this.setData({rows:await api.call("listChildProfileRequests",{status:"PENDING_REVIEW"}),loading:false});}finally{wx.stopPullDownRefresh();}},review(e){const id=e.currentTarget.dataset.id,decision=e.currentTarget.dataset.decision;wx.showModal({title:decision==="REJECT"?"退回资料":"确认学员资料",content:decision==="REJECT"?"退回后家长需要修正资料。":"如发现重复档案，系统将优先绑定已有学员；否则创建0课时、未分班的新档案。",success:async r=>{if(!r.confirm)return;await api.call("reviewChildProfileRequest",{id,decision});wx.showToast({title:decision==="REJECT"?"已退回":"已确认"});this.load();}});}});
